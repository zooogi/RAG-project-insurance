# 快速参考指南

## 📁 数据目录快速查找

### 我想查看...

| 需求 | 查看位置 |
|------|---------|
| OCR处理结果 | `data/processed/**/*.md` |
| 文本清洗效果 | `data/cleaned/**/*.md`（对比 `processed/**/*.md`） |
| 最终chunk数据 | `data/chunks/*_chunks.json`（排除 `test_*.json`） |
| 原始输入文件 | `data/raw_data/` |

### 文件类型速查

| 文件/目录 | 是什么 | 用途 | 可删除？ |
|----------|--------|------|---------|
| `raw_data/*.pdf` | 原始PDF | 输入数据 | ❌ 不可删除 |
| `processed/**/*.md` | OCR输出 | 后续处理的基础 | ❌ 不可删除 |
| `cleaned/**/*.md` | 清洗后的文本 | 查看清洗效果 | ✅ 可删除 |
| `chunks/*_chunks.json` | Chunk数据 | 用于embedding | ❌ 不可删除（生产数据） |
| `chunks/test_*.json` | 测试数据 | 测试产物 | ✅ 可删除 |
| `mineru_test/` | 测试输出 | MineRU测试 | ✅ 可删除 |

### 处理流程

```
1. 放入原始文件
   → data/raw_data/*.pdf

2. OCR处理
   → data/processed/**/*.md

3. 文本清洗（可选）
   → data/cleaned/**/*.md

4. Chunker分块
   → data/chunks/*_chunks.json ⭐

5. Embedding
   → 读取 chunks/*_chunks.json
```

### 常见问题

**Q: 哪些文件是最终用于embedding的？**
A: `data/chunks/*_chunks.json` 文件中的 `embedding_text` 字段

**Q: 如何重新生成chunks？**
A: 删除对应的 `chunks/*_chunks.json` 文件，重新运行chunker

**Q: 测试文件可以删除吗？**
A: 可以，`test_*.json` 和 `test_*.md` 都是测试产物

**Q: 如何清理空间？**
A: 删除 `cleaned/`、`mineru_test/`、OCR中间文件、测试文件

详细说明：[DATA_STRUCTURE.md](DATA_STRUCTURE.md)
